{% extends 'base.html.twig' %}

{% block title %}Materiel{% endblock %}

{% block body %}
    <div class="m-5 card" >
    <div class="card-header">
        <div class="h-100 w-100 d-flex align-items-center justify-content-between">
            <h4 class="font-weight-bold text-danger">Informations générales</h4>
        </div>
    </div>
    <div class="card-body p-4" >
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-danger">Valider les modification</button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
